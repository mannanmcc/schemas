syntax="proto3";

package rpc.order;

import "order.proto";

option go_package = "github.com/schemas/build/go/rpc/order";

//OrderService is the service that place an order
service OrderService {
  rpc PlaceOrder(OrderRequest) returns (OrderResponse);
}